#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define SIZE 10
int main() {
    int a[SIZE];

    for(int i=0;i<SIZE;i++) 
        a[i]=rand();
    
    printf("Unsorted Array\n");
    for(int i=0;i<SIZE;i++) 
        printf("%d,",a[i]);

    int temp;
    bool swap=true;
    for(;swap;) {
        for(int j=0;j<SIZE;j++){

        }
    }
}